import { defineConfig } from "vite";

export default defineConfig({
  // ReScript compiles in-source to src/*.res.js (ESModules)
  // Vite picks up index.html at the project root as the entry point
});
